# Clean ablation: the complete DPHC FullResContext module at stride 2.
#
# The DPHC architecture, Density VFE, Pedestrian-only head, optimizer,
# batch size and 50-epoch budget are inherited unchanged. The only structural
# change is that FullResContext consumes block-1 features and its anchors use
# feature_map_stride=2.

_BASE_CONFIG_: cfgs/kitti_models/pointpillar_dphc.yaml

# OpenPCDet only expands one _BASE_CONFIG_ level. The parent DPHC config
# references kitti_dataset.yaml, so materialise its loader fields here.
DATA_CONFIG:
    DATASET: KittiDataset
    DATA_PATH: ../data/kitti
    DATA_SPLIT: {
        'train': train,
        'test': val
    }
    INFO_PATH: {
        'train': [kitti_infos_train.pkl],
        'test': [kitti_infos_val.pkl]
    }
    GET_ITEM_LIST: [points]
    FOV_POINTS_ONLY: True
    POINT_FEATURE_ENCODING: {
        encoding_type: absolute_coordinates_encoding,
        used_feature_list: ['x', 'y', 'z', 'intensity'],
        src_feature_list: ['x', 'y', 'z', 'intensity']
    }

MODEL:
    SMALL_OBJECT_AUX:
        AUX_INPUT_SOURCE: block1
        FEATURE_MAP_STRIDE: 2

OPTIMIZATION:
    BATCH_SIZE_PER_GPU: 2
    NUM_EPOCHS: 50
